/** Lock app after 5 minutes in background. */
export const BACKGROUND_LOCK_MS = 5 * 60 * 1000;

/** Lock app after 15 minutes of foreground idle. */
export const FOREGROUND_IDLE_LOCK_MS = 15 * 60 * 1000;
